# Plan 04-01: Supporting Components — equipment, about, differentials, process, coverage

## Goal
Connect equipment, about, differentials, process, and coverage-map components to config.

## Tasks
1. Update `equipment.tsx`: read `equipment` array from config, use `getIcon()`
2. Update `about.tsx`: read `about` config (credentials, stats, paragraphs)
3. Update `differentials.tsx`: read `differentials` array from config, use `getIcon()`
4. Update `process.tsx`: read `process` array from config, use `getIcon()`
5. Update `coverage-map.tsx`: read `coverage` array from config

## Verification
- All sections render from config data
- Changing config data updates rendered content
