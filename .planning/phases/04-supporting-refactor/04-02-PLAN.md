# Plan 04-02: Supporting Components — projects, testimonials, FAQ, calculator, blog, google-rating, client-logos

## Goal
Connect projects, testimonials, FAQ, calculator, blog, google-rating, and client-logos to config.

## Tasks
1. Update `projects.tsx`: read `projects` array from config
2. Update `testimonials.tsx`: read `testimonials` array from config
3. Update `faq.tsx`: read `faq` array from config
4. Update `calculator.tsx`: read `calculator` config (serviceOptions, purposeOptions, pricing)
5. Update `blog-preview.tsx`: read `blog` array from config
6. Update `google-rating.tsx`: read `googleRating` config
7. Update `client-logos.tsx`: read `clientLogos` config

## Verification
- All sections render from config data
- Calculator uses configurable pricing
