# Plan 02-01: Colors Config + CSS Variables

## Goal
Add a `colors` section to `site.config.ts` and wire CSS variables in `globals.css` so all site colors are driven by config.

## Tasks
1. Add `ColorsSchema` to `lib/config-types.ts` (10 color fields: primary, foreground, background, muted, secondary, destructive, border, topoLine, topoAccent, backgroundAlt)
2. Add `colors` section to `site.config.ts` with GeoTech demo values
3. Update `globals.css`: add missing CSS variables (--topo-accent, --background-alt) and map them in `@theme inline`

## Verification
- Config validates with Zod
- CSS variables available for Tailwind utility classes
- No component changes yet (Phase 2-02)
