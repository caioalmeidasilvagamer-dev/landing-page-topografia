# Plan 02-02: Migrate Components to CSS Variables

## Goal
Replace all hardcoded hex colors in components with Tailwind CSS utility classes that reference the CSS variables.

## Tasks
1. Migrate `footer.tsx` — bg-foreground, text-topo-accent, bg-topo-line, border-topo-line/30
2. Migrate `contact-form.tsx` — bg-foreground, text-topo-accent, text-destructive, border-destructive
3. Migrate `navbar.tsx` — bg-primary, text-primary, SVG stroke var(--primary)
4. Migrate `hero.tsx` — bg-background, var(--background) gradients
5. Migrate `differentials.tsx` — stroke var(--primary), bg-muted, border-border
6. Migrate `process.tsx` — bg-border, bg-primary, border-primary
7. Migrate `faq.tsx`, `testimonials.tsx`, `services.tsx`, `about.tsx`, `equipment.tsx`, `coverage-map.tsx`, `projects.tsx` — bg-muted, border-secondary, border-border
8. Migrate `calculator.tsx`, `topo-page-background.tsx` — border-l-primary, var(--background)
9. Remove `ignoreBuildErrors: true` from `next.config.mjs` (restored due to pre-existing TS errors)

## Verification
- `grep -r '#[0-9A-Fa-f]\{6\}' components/` returns only blog-preview category colors and google-rating brand colors (non-migratable)
- All components use Tailwind utility classes for colors
