# Phase 6-01: Schema + Config Updates

## Goal
Add all new config fields to Zod schemas and populate site.config.ts with data

## Tasks

### Task 1: Extend HeroSchema with certifications
**File:** `lib/config-types.ts`
**Change:** Add `certifications` array to HeroSchema
```ts
export const HeroCertificationSchema = z.object({
  code: z.string().min(1),
  label: z.string().min(1),
})

// In HeroSchema, add:
certifications: z.array(HeroCertificationSchema).optional().default([]),
```

### Task 2: Extend FooterSchema with description + certifications
**File:** `lib/config-types.ts`
**Change:** Add footer-level fields
```ts
// Create FooterConfigSchema
export const FooterConfigSchema = z.object({
  description: z.string().optional(),
  certifications: z.array(z.object({
    code: z.string().min(1),
    label: z.string().min(1),
  })).optional().default([]),
})
```

### Task 3: Extend ServiceSchema section with headline/description
**File:** `lib/config-types.ts`
**Change:** Create ServicesConfigSchema for section-level config
```ts
export const ServicesConfigSchema = z.object({
  headline: z.string().optional(),
  description: z.string().optional(),
  items: z.array(ServiceSchema),
})
```

### Task 4: Extend AboutSchema with headline
**File:** `lib/config-types.ts`
**Change:** Add headline field
```ts
// In AboutSchema, add:
headline: z.string().optional(),
```

### Task 5: Extend ContactSchema with coverageRegions
**File:** `lib/config-types.ts`
**Change:** Add coverageRegions array
```ts
// In ContactSchema, add:
coverageRegions: z.array(z.string()).optional().default([]),
```

### Task 6: Create FaqConfigSchema with certifications
**File:** `lib/config-types.ts`
**Change:** Add certifications to FAQ section
```ts
export const FaqConfigSchema = z.object({
  certifications: z.array(z.string()).optional().default([]),
  items: z.array(FaqSchema),
})
```

### Task 7: Update SiteConfigSchema to use new types
**File:** `lib/config-types.ts`
**Change:** Replace flat arrays with config objects
```ts
// In SiteConfigSchema:
services: ServicesConfigSchema, // was: z.array(ServiceSchema)
faq: FaqConfigSchema, // was: z.array(FaqSchema)
footer: FooterConfigSchema, // NEW
```

### Task 8: Export new types
**File:** `lib/config-types.ts`
**Change:** Add type exports
```ts
export type HeroCertification = z.infer<typeof HeroCertificationSchema>
export type FooterConfig = z.infer<typeof FooterConfigSchema>
export type ServicesConfig = z.infer<typeof ServicesConfigSchema>
export type FaqConfig = z.infer<typeof FaqConfigSchema>
```

### Task 9: Update site.config.ts with new fields
**File:** `site.config.ts`
**Change:** Add certifications to hero, footer config, services headline, etc.

### Task 10: Update site.config.demo.ts with new fields
**File:** `site.config.demo.ts`
**Change:** Add demo data for all new fields

## Verification
- [ ] All new Zod schemas pass validation
- [ ] site.config.ts compiles without errors
- [ ] site.config.demo.ts compiles without errors
- [ ] TypeScript types are correctly inferred
