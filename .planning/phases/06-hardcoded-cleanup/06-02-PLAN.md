# Phase 6-02: Component Refactor

## Goal
Refactor all components to use the new config fields instead of hardcoded values

## Tasks

### Task 1: Refactor hero.tsx certifications
**File:** `components/hero.tsx`
**Change:** Replace hardcoded certifications (lines 222-226) with config
```tsx
// Before:
{[
  { code: 'NBR 13.133', label: 'Levantamento Topográfico' },
  { code: 'INCRA 572', label: 'Georreferenciamento Rural' },
  { code: 'ISO 9001', label: 'Gestão da Qualidade' },
  { code: 'CREA/CAU', label: 'Habilitação Profissional' },
].map(...)

// After:
{h.certifications.map((cert) => (...))}
```

### Task 2: Refactor footer.tsx
**File:** `components/footer.tsx`
**Changes:**
1. Replace hardcoded description (line 42) with `footerConfig.description`
2. Replace hardcoded certifications (lines 139-144) with `footerConfig.certifications`
```tsx
const footerConfig = config.footer
// Use footerConfig.description and footerConfig.certifications
```

### Task 3: Refactor services.tsx
**File:** `components/services.tsx`
**Changes:**
1. Replace hardcoded headline (line 93) with `config.services.headline`
2. Replace hardcoded description (lines 103-104) with `config.services.description`
3. Update service array access from `config.services` to `config.services.items`
```tsx
const { headline, description, items } = config.services
// Use headline, description, and items instead of config.services
```

### Task 4: Refactor about.tsx
**File:** `components/about.tsx`
**Change:** Replace hardcoded headline (line 49) with `config.about.headline`
```tsx
<h2>{a.headline}</h2>
```

### Task 5: Refactor contact-form.tsx
**File:** `components/contact-form.tsx`
**Change:** Replace hardcoded coverage regions (lines 384-389) with config
```tsx
{config.contact.coverageRegions.map((region) => (...))}
```

### Task 6: Refactor faq.tsx
**File:** `components/faq.tsx`
**Changes:**
1. Replace hardcoded certifications (lines 111-115) with `config.faq.certifications`
2. Update FAQ array access from `config.faq` to `config.faq.items`
```tsx
const { certifications, items } = config.faq
```

### Task 7: Update all component imports
**Files:** All modified components
**Change:** Ensure imports match new config structure

## Verification
- [ ] No hardcoded Portuguese text remains in components
- [ ] All text configurable via config
- [ ] Site renders correctly with current config
- [ ] Site renders correctly with demo config
